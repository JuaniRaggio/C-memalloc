#include "intaritmetic.h"
/* abs: Returns the absolute value of an integer. */
int abs(int);

/* labs: Returns the absolute value of a long. */
long labs(long);

/* llabs: Returns the absolute value of a long long. */
long long llabs(long long);
